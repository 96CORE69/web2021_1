<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>MBの選択</h2>
  <td>-選択中のパーツ-</td><br>
  <td>【CPU】</td>
        <% for( let row of data_CPU ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.model %></td>
            <td>　　</td>
            <td><%= row.price %></td>
        </tr><br><br>
      <% } %>
  <table border="1">
  <td>ID</td>
  <td>型番</td>
  <td>グレード</td>
  <td>ソケット</td>
  <td>価格</td>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.model %></td>
            <td><%= row.grade %></td>
            <td><%= row.socket %></td>
            <td><%= row.price %></td>
        </tr>
      <% } %>
  </table>
  <br>
  <td>使用するIDを入力してください</td><br>
        <form action="/MEM">
            <input type="integer" name="MB_ID" id="MB_ID">
            <input type="hidden" name="CPU_ID" id="CPU_ID" value=<%= data_CPU.id %> > 
            <input type="submit" value="送信">
        </form>
   <a href="/">トップへ戻る</a>
   <td>　　</td>
   <a href="/CPU">ひとつ前に戻る</a>
   <a href="/DELETE">編集</a>
</body>
</html>